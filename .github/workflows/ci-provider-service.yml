name: CI — Provider Service

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'services/provider-service/**'
      - '.github/workflows/ci-provider-service.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'services/provider-service/**'
      - '.github/workflows/ci-provider-service.yml'

defaults:
  run:
    working-directory: services/provider-service

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: services/provider-service/go.sum

      - name: Download dependencies
        run: go mod download

      - name: Debug - verify dependencies
        run: |
          go mod verify
          go build ./...

      - name: Clean golangci-lint cache
        run: |
          mkdir -p ~/.cache/golangci-lint
          rm -rf ~/.cache/golangci-lint/*

      - name: go vet (fallback вместо golangci-lint)
        run: go vet ./...

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: services/provider-service/go.sum

      - name: Download dependencies
        run: go mod download

      - name: Run tests with coverage
        run: |
          go test ./... \
            -v \
            -race \
            -count=1 \
            -coverprofile=coverage.out \
            -covermode=atomic
        
      - name: Show coverage details
        run: |
          echo "=== Full coverage report ==="
          go tool cover -func=coverage.out
          echo "=== Total line ==="
          go tool cover -func=coverage.out | grep "^total:"
          echo "=== Extracted value ==="
          go tool cover -func=coverage.out | grep "^total:" | awk '{print $3}'

      - name: Check coverage threshold
        run: |
          COVERAGE=$(go tool cover -func=coverage.out \
            | grep "^total:" \
            | awk '{print $3}' \
            | sed 's/%//')
          
          echo "Total coverage: ${COVERAGE}%"
          
          # bc корректно работает с float
          BELOW=$(echo "${COVERAGE} < 55" | bc -l)
          if [ "$BELOW" -eq 1 ]; then
            echo "FAIL: ${COVERAGE}% < 55%"
            exit 1
          fi
          echo "OK: ${COVERAGE}% >= 55%"

  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: [ lint, test ]
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build \
            --tag provider-service:${{ github.sha }} \
            --file Dockerfile \
            .

      - name: Check image size
        run: |
          SIZE=$(docker image inspect provider-service:${{ github.sha }} \
            --format='{{.Size}}')
          SIZE_MB=$((SIZE / 1024 / 1024))
          echo "Image size: ${SIZE_MB}MB"
          if [ "$SIZE_MB" -gt 100 ]; then
            echo "Image too large: ${SIZE_MB}MB > 100MB"
            exit 1
          fi
